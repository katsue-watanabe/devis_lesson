<% provide(:title, 'All Articles') %>
<h1>記事一覧</h1>
<%= link_to "新規作成", new_users_article_path(id: current_user.id), class: "btn btn-primary" %>
<div class="col-md-10 col-md-offset-1">
  <table class="table table-condensed table-hover" id="table-articles">
    <thead>
      <tr>
        <th>詳細表示</th>
        <th>タイトル</th>
        <th>内容</th>
        <th>画像</th>
        <th>編集・削除</th>
      </tr>
    </thead>
    <body>
      <% @articles.each do |article| %>
        <tr>
          <td><%= link_to "詳細", users_article_path(article), class: "btn btn-primary" %></td>
          <td><%= article.title %></td>
          <td><%= article.content %></td>
          <%
=begin%>
 <td><%= image_tag article.image.url %></td> 
<%
=end%>
          <td>
            <%= link_to "編集", edit_users_article_path(article), class: "btn btn-success" %>
            <%= link_to "削除",  users_article_path(article), method: :delete, data: { confirm: "削除してもよろしいですか？" }, class: "btn btn-danger" %>
          </td>
        </tr>
      <% end %> 
    </body>
  </table>
</div>